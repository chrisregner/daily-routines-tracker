import chai, { expect } from 'chai'
import td from 'testdouble'
// import rndRange from './rndRange'


describe.skip('rndRange()', () => {
  it('should return a function', () => {
    expect(rndRange()).to.be.a('function')
  })

  describe('the returned function', () => {
    it('should return a range that is within the passed range argument')
  })
})