import * as actions from './TaskFormActions'

describe('TaskForm actions', () => {
  it('can create an action to add a Task')
  it('can create an action to update a Task')
  it('can create an action to delete a Task')
})