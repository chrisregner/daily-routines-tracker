import * as actions from './RoutineFormActions'

describe('RoutineForm actions', () => {
  it('can create an action to add a routine')
  it('can create an action to update a routine')
  it('can create an action to delete a routine')
})