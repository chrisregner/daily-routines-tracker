/**
 * object whose keys are the virtual classes and values are their classes
 * (which can be another virtual) equivalent
 * @example
 * {
 *   '-virtual-class-a': 'func-class-a func-class-b',
 *   '-virtual-class-b': 'virtual-class-a func-class-c'
 * }
 * @type {Object}
 */
export default {
  '-btn-reset': 'pa0 ma0 bn bg-transparent outline-0 pointer',
}
